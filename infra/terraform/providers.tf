# The Scaleway provider reads credentials, organization, project, region,
# and zone from the SCW_* environment variables automatically. We leave
# this block empty so nothing sensitive ever lands in configuration files.
# See CLAUDE.md "Environment & secrets" for the variable list.
provider "scaleway" {}
